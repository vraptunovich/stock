# List of stock tickers to download options for
tickers:
  - AAPL
  - GOOG
  - AMZN
  - LLY
  - JPM
  - META
  - NVDA
  - PLTR
  - TSM
  - NFLX
  - XOM

# Optional expiration date range (inclusive).
# Can be "YYYY-MM-DD", "DD-MM-YYYY", or empty string "" if not used.
exp_start:
exp_end:

# Optional explicit expiration dates. If non-empty, this list has priority
# over exp_start / exp_end.
# Example: ["2025-10-17", "10-10-2025"]
exp_dates: []

# Directory (relative to repo root) where CSV files will be stored.
# This directory will also be uploaded as an artifact in GitHub Actions.
outdir: "csv_out"

snap_date:


spot_price_overrides:
  SPY: 520.10


portfolio_file_name: equity_vanilla_portfolio_1.csv
valuation_date:

